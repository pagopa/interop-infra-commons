name: Terraform Checks
run-name: "Terraform Checks - ${{ github.ref_name }}"

on:
  pull_request:
    paths:
      - "terraform/modules/**"

env:
  TFLINT_VERSION: "v0.61.0"
  TF_PLUGIN_CACHE_DIR: /home/runner/.terraform.d/plugin-cache

permissions:
  contents: read

jobs:
  discover-modules:
    name: Discover Terraform Modules
    runs-on: ubuntu-24.04
    outputs:
      matrix: ${{ steps.build-matrix.outputs.matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Build module matrix
        id: build-matrix
        run: |
          set -euo pipefail
 
          matrix_entries="[]"

          while IFS= read -r module_dir; do
            # Skip empty module folders
            if [ -z "$(find "$module_dir" -mindepth 1 -print -quit)" ]; then
              continue
            fi

            module_name=$(basename "$module_dir")

            matrix_entries=$(echo "$matrix_entries" | jq -c \
                --arg path "$module_dir" \
                --arg name "$module_name" \
                '. + [{"path": $path, "name": $name }]')

          done < <(find terraform/modules -mindepth 1 -maxdepth 1 -type d | sort)
          
          echo "matrix={\"include\":$(echo "$matrix_entries")}" >> "$GITHUB_OUTPUT"

  terraform-checks:
    name: "[${{ matrix.name }}] Terraform Checks"
    needs: [discover-modules]
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.discover-modules.outputs.matrix) }}
      max-parallel: 10
    defaults:
      run:
        working-directory: ${{ matrix.path }}

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Compute Terraform version
        id: compute-tf-version
        run: |
          set -euo pipefail

          # grep:
          #  -h Suppress the prefixing of file names on output. 
          #  -r Recursive search
          # sed:
          #  -E Extended regex: The regex captures the version string in required_version = "x.y.z"
          constraint=$(grep -rh --include='*.tf' 'required_version' . 2>/dev/null \
            | sed -E 's/.*required_version *= *"([^"]+)".*/\1/' \
            | head -1 || true)

          if [ -z "$constraint" ]; then
            echo "::error::No required_version constraint found for module '${{ matrix.name }}'"
            exit 1
          else
            echo "${{ matrix.name }} Constraint found: $constraint"
            # Trim whitespace and set output           
            echo "tf_version=$(echo "$constraint" | xargs)" >> "$GITHUB_OUTPUT"
          fi

      - name: Ensure plugin cache dir exists
        run: mkdir -p /home/runner/.terraform.d/plugin-cache

      - name: Terraform cache
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
        with:
          path: |
            /home/runner/.terraform.d/plugin-cache
          key: tf-${{ runner.os }}-${{ steps.compute-tf-version.outputs.tf_version }}

      - name: "Setup Terraform ${{ steps.compute-tf-version.outputs.tf_version }}"
        uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3.1.2
        with:
          terraform_version: ${{ steps.compute-tf-version.outputs.tf_version }}

      - name: Show Terraform version
        run: terraform version

      - name: terraform fmt
        id: fmt
        run: terraform fmt -check -recursive -diff
        # -check: exit 1 when files need formatting (non-destructive)
        # -diff:  print the diff so reviewers see what needs fixing

      - name: terraform init
        id: init
        run: |
          terraform init \
            -backend=false \
            -input=false \
            -no-color
        # -backend=false: skip remote-state init

      - name: terraform validate
        id: validate
        run: terraform validate -no-color

      - name: Setup TFLint ${{ env.TFLINT_VERSION }}
        uses: terraform-linters/setup-tflint@6770de8d186019a148cc2b144e8bfa627f7d4aa8
        with:
          tflint_version: ${{ env.TFLINT_VERSION }}

      - name: Cache TFLint plugins
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
        with:
          path: /home/runner/.tflint.d/plugins
          key: tflint-${{ env.TFLINT_VERSION }}-${{ hashFiles('**/.tflint.hcl') }}
          restore-keys: |
            tflint-${{ env.TFLINT_VERSION }}-
            tflint-

      - name: Init TFLint
        run: |
          # AWS Tflint rules: https://github.com/terraform-linters/tflint-ruleset-aws/blob/master/docs/rules/README.md
          # Use module-local .tflint.hcl when present; otherwise copy the repo-level config
          # into the module folder so `tflint --init` can download the required plugins.

          if [ -f .tflint.hcl ] || [ -f "$GITHUB_WORKSPACE/.tflint.hcl" ]; then
            if [ ! -f .tflint.hcl ] && [ -f "$GITHUB_WORKSPACE/.tflint.hcl" ]; then
              cp "$GITHUB_WORKSPACE/.tflint.hcl" .
            fi
            tflint --init
          fi

      - name: Run TFLint
        id: lint
        run: tflint --format compact --no-color

  terraform-ci-gate:
    name: Terraform CI Gate
    if: always()
    needs: terraform-checks
    runs-on: ubuntu-24.04
    steps:
      - name: Check matrix jobs status
        run: |
          result="${{ needs.terraform-checks.result }}"
          if [[ "$result" != "success" ]]; then
            echo "::error title=Terraform CI Gate::One or more Terraform module checks failed (result: $result)"
            exit 1
          fi
          echo "Terraform CI Gate: all Terraform module checks passed."